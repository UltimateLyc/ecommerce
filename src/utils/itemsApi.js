// Funcion para el URL del API

const getUrlItems = () => {
  const urlItems = 'https://ecomerce-master.herokuapp.com/api/v1/item'
  console.log('🚀 ~ urlItems', urlItems)

  return urlItems
}

export default getUrlItems
